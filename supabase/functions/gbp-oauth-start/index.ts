import { createClient, type SupabaseClient } from 'https://esm.sh/@supabase/supabase-js@2'
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type'}
const json=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:{...cors,'content-type':'application/json'}})
function clients(request:Request){const url=Deno.env.get('SUPABASE_URL')!,anon=Deno.env.get('SUPABASE_ANON_KEY')!,service=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!,authorization=request.headers.get('Authorization');if(!authorization)throw new Error('Unauthorized');return{caller:createClient(url,anon,{global:{headers:{Authorization:authorization}}}),admin:createClient(url,service)}}
async function authorizedUser(caller:SupabaseClient,admin:SupabaseClient,facilityId:string,write=true){const{data:{user}}=await caller.auth.getUser();if(!user)throw new Error('Unauthorized');const{data:profile}=await admin.from('profiles').select('role').eq('id',user.id).single();if(profile?.role==='admin')return user;if(write&&profile?.role==='consultant')throw new Error('Consultants have read-only access');const{data}=await admin.from('facility_users').select('id').eq('facility_id',facilityId).eq('user_id',user.id).maybeSingle();if(!data)throw new Error('Facility access denied');return user}
function oauthConfig(){const clientId=Deno.env.get('GOOGLE_CLIENT_ID'),redirectUri=Deno.env.get('GOOGLE_REDIRECT_URI'),appUrl=Deno.env.get('APP_URL');if(!clientId||!redirectUri||!appUrl)throw new Error('Google OAuth secrets are not configured');return{clientId,redirectUri,appUrl:appUrl.replace(/\/$/,'')}}
Deno.serve(async request=>{if(request.method==='OPTIONS')return new Response('ok',{headers:cors});try{const{facility_id}=await request.json() as {facility_id:string};if(!facility_id)throw new Error('facility_id is required');const{caller,admin}=clients(request),user=await authorizedUser(caller,admin,facility_id,true),config=oauthConfig(),state=`${crypto.randomUUID()}${crypto.randomUUID()}`;const{error}=await admin.from('gbp_oauth_states').insert({state,facility_id,user_id:user.id,return_url:`${config.appUrl}/settings`});if(error)throw error;const params=new URLSearchParams({client_id:config.clientId,redirect_uri:config.redirectUri,response_type:'code',scope:'https://www.googleapis.com/auth/business.manage',access_type:'offline',include_granted_scopes:'true',prompt:'consent',state});return json({url:`https://accounts.google.com/o/oauth2/v2/auth?${params}`})}catch(error){return json({error:error instanceof Error?error.message:'OAuth start failed'},400)}})
