import type { SupabaseClient } from 'https://esm.sh/@supabase/supabase-js@2'
export async function gbpAccess(admin:SupabaseClient,facilityId:string){const {data:connection,error}=await admin.from('gbp_connections').select('account_id,location_id,refresh_token').eq('facility_id',facilityId).single();if(error)throw new Error('GBP connection not found');const clientId=Deno.env.get('GOOGLE_CLIENT_ID'),clientSecret=Deno.env.get('GOOGLE_CLIENT_SECRET');if(!clientId||!clientSecret)throw new Error('Google OAuth secrets are not configured');const response=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body:new URLSearchParams({client_id:clientId,client_secret:clientSecret,refresh_token:connection.refresh_token,grant_type:'refresh_token'})});if(!response.ok)throw new Error(`Google token refresh failed: ${response.status}`);const token=await response.json() as {access_token:string};return {...connection,accessToken:token.access_token}}
export const starValue:Record<string,number>={ONE:1,TWO:2,THREE:3,FOUR:4,FIVE:5}
