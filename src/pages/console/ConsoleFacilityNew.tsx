import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { useCreateFacility } from '../../features/console/use-console'
export default function ConsoleFacilityNew(){const create=useCreateFacility(),navigate=useNavigate(),[name,setName]=useState(''),[prefecture,setPrefecture]=useState(''),[category,setCategory]=useState('ryokan'),[userName,setUserName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[note,setNote]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();const result=await create.mutateAsync({facility:{name,prefecture,category},user:{name:userName,email,password},draft_note:note});navigate(`/console/${result.facility_id}/onboarding`)};return <section className="role-page"><header><span>CONSULTANT</span><h1>新規施設登録</h1><p>施設とオーナーアカウントを同時に作成します。</p></header><form className="facility-form" onSubmit={e=>void submit(e)}><fieldset><legend>施設情報</legend><label>施設名<input required value={name} onChange={e=>setName(e.target.value)}/></label><div><label>都道府県<input required value={prefecture} onChange={e=>setPrefecture(e.target.value)}/></label><label>カテゴリ<select value={category} onChange={e=>setCategory(e.target.value)}><option value="ryokan">旅館</option><option value="hotel">ホテル</option><option value="guesthouse">ゲストハウス</option><option value="other">その他</option></select></label></div></fieldset><fieldset><legend>施設担当者</legend><label>担当者名<input required value={userName} onChange={e=>setUserName(e.target.value)}/></label><div><label>メールアドレス<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>初期パスワード<input required type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)}/></label></div></fieldset><fieldset><legend>ヒアリングメモ（任意）</legend><label>引き継ぎ事項<textarea rows={5} value={note} onChange={e=>setNote(e.target.value)}/></label></fieldset>{create.error&&<p className="data-error">{create.error.message}</p>}<button disabled={create.isPending}>{create.isPending?'登録中…':'施設とユーザーを登録する'}</button></form></section>}
