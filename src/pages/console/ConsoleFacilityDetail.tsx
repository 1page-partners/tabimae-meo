import { Link, useParams } from 'react-router-dom'
import { useFacility } from '../../features/console/use-console'
export default function ConsoleFacilityDetail(){const {facilityId}=useParams(),{data,isLoading,error}=useFacility(facilityId);return <section className="role-page"><header><span>CONSULTANT・読み取り専用</span><h1>{isLoading?'読み込み中…':data?.name??'施設詳細'}</h1><p>{data?.prefecture}・{data?.category}</p></header>{error&&<p className="data-error">施設情報を取得できません。</p>}<div className="detail-actions"><Link to={`/console/${facilityId}/reviews`}>口コミ状況</Link><Link to={`/console/${facilityId}/onboarding`}>ヒアリング編集</Link></div><div className="stat-grid"><article className="stat-card"><span>GBP連携</span><strong className="small-value">{data?.gbp_place_id?'連携済み':'未連携'}</strong></article><article className="stat-card"><span>施設ID</span><strong className="small-value">{facilityId}</strong></article></div></section>}
