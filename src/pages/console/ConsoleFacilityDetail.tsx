import { BarChart3, ClipboardList, ExternalLink, MessageSquareText } from 'lucide-react'
import { Link, useLocation, useParams } from 'react-router-dom'
import { useFacility } from '../../features/console/use-console'
export default function ConsoleFacilityDetail(){const{facilityId}=useParams(),isAdmin=useLocation().pathname.startsWith('/admin'),base=isAdmin?`/admin/facilities/${facilityId}`:`/console/${facilityId}`,{data,isLoading,error}=useFacility(facilityId);return <section className="role-page"><header><span>{isAdmin?'ADMIN':'CONSULTANT'}・施設管理</span><h1>{isLoading?'読み込み中…':data?.name??'施設詳細'}</h1><p>{data?.prefecture}・{data?.category}</p></header>{error&&<p className="data-error">施設情報を取得できません。</p>}<div className="facility-action-grid"><Link to={`${base}/workspace`}><BarChart3 size={20}/><div><strong>施設ページを見る</strong><small>実データのダッシュボードを表示</small></div><ExternalLink size={15}/></Link><Link to={`${base}/reviews`}><MessageSquareText size={20}/><div><strong>口コミ状況</strong><small>口コミと返信状況を確認</small></div><ExternalLink size={15}/></Link><Link to={`${base}/onboarding`}><ClipboardList size={20}/><div><strong>ヒアリング編集</strong><small>施設情報と運用目標を編集</small></div><ExternalLink size={15}/></Link></div><div className="stat-grid"><article className="stat-card"><span>GBP連携</span><strong className="small-value">{data?.gbp_place_id?'連携済み':'未連携'}</strong></article><article className="stat-card"><span>施設ID</span><strong className="small-value">{facilityId}</strong></article></div></section>}
