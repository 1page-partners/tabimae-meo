import { useState } from 'react'
import { Navigate } from 'react-router-dom'
import { REDIRECT_AFTER_LOGIN } from '../features/auth/redirects'
import { useAuth } from '../features/auth/use-auth'
export default function Login(){const {user,signIn}=useAuth(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);if(user)return <Navigate to={REDIRECT_AFTER_LOGIN[user.role]} replace/>;const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await signIn(email,password)}catch(reason){setError(reason instanceof Error?reason.message:'ログインに失敗しました')}finally{setBusy(false)}};return <main className="auth-page"><form className="auth-card" onSubmit={submit}><div className="auth-brand"><span className="brand-mark">M</span><strong>タビマエMEO</strong></div><div><h1>ログイン</h1><p>管理者・コンサルタント・施設担当者共通のログイン画面です。</p></div><label>メールアドレス<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>パスワード<input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="auth-error">{error}</p>}<button disabled={busy}>{busy?'ログイン中…':'ログイン'}</button></form></main>}
