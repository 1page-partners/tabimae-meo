import { Link } from 'react-router-dom'
import { useFacilities } from '../../features/admin/use-admin'
export default function AdminFacilities(){const {data=[],isLoading}=useFacilities();return <section className="role-page"><header><span>ADMIN</span><h1>全施設</h1><p>登録施設を横断して確認します。</p></header><div className="data-table"><div className="data-row facility-row head"><span>施設名</span><span>都道府県</span><span>カテゴリ</span><span/></div>{isLoading?<p className="loading-row">読み込み中…</p>:data.map(item=><div className="data-row facility-row" key={item.id}><strong>{item.name}</strong><span>{item.prefecture||'—'}</span><span>{item.category||'—'}</span><Link to={`/admin/facilities/${item.id}`}>詳細を見る</Link></div>)}{!isLoading&&!data.length&&<p className="loading-row">施設はまだ登録されていません。</p>}</div></section>}
