import { useProfiles } from '../../features/admin/use-admin'
export default function AdminUsers(){const {data=[],isLoading}=useProfiles();return <section className="role-page"><header><span>ADMIN</span><h1>ユーザー管理</h1><p>全ユーザーのロールと登録状況を確認します。</p></header><div className="data-table"><div className="data-row head"><span>氏名</span><span>メール</span><span>ロール</span></div>{isLoading?<p className="loading-row">読み込み中…</p>:data.map(item=><div className="data-row" key={item.id}><strong>{item.name||'名称未設定'}</strong><span>{item.email}</span><span className="pill">{item.role}</span></div>)}</div></section>}
