import { Plus } from 'lucide-react'
import { useState } from 'react'
import { useInviteConsultant, useProfiles } from '../../features/admin/use-admin'
export default function AdminConsultants(){const {data=[],isLoading}=useProfiles('consultant'),invite=useInviteConsultant(),[open,setOpen]=useState(false),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();await invite.mutateAsync({name,email,password});setOpen(false);setName('');setEmail('');setPassword('')};return <section className="role-page"><header className="role-heading"><div><span>ADMIN</span><h1>コンサルタント管理</h1><p>担当者の招待とアカウント管理を行います。</p></div><button onClick={()=>setOpen(!open)}><Plus size={15}/>新規招待</button></header>{open&&<form className="inline-form" onSubmit={e=>void submit(e)}><label>氏名<input required value={name} onChange={e=>setName(e.target.value)}/></label><label>メールアドレス<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>初期パスワード<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{invite.error&&<p className="data-error">{invite.error.message}</p>}<button disabled={invite.isPending}>{invite.isPending?'作成中…':'招待する'}</button></form>}<div className="data-table"><div className="data-row head"><span>氏名</span><span>メール</span><span>登録日</span></div>{isLoading?<p className="loading-row">読み込み中…</p>:data.map(item=><div className="data-row" key={item.id}><strong>{item.name||'名称未設定'}</strong><span>{item.email}</span><small>{new Date(item.created_at).toLocaleDateString('ja-JP')}</small></div>)}{!isLoading&&!data.length&&<p className="loading-row">コンサルタントはまだ登録されていません。</p>}</div></section>}
